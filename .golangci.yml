version: "2"

run:
  timeout: 3m

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
